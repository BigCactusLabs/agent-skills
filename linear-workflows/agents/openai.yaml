interface:
  display_name: "Linear Workflows"
  short_description: "Use Linear connector tools safely"
  default_prompt: "Use $linear-workflows to inspect Linear context, choose safe connector tools, and avoid broken Linear MCP surfaces."

dependencies:
  tools:
    - type: "mcp"
      value: "linear"
      description: "Linear MCP/app connector"
      transport: "streamable_http"
      url: "https://mcp.linear.app/mcp"
