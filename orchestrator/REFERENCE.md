# Orchestrator — cold-path reference

Material consulted rarely: read the section you need, not the file. Hot-path mechanics (dispatch, supervise, resume, pools) live in `SKILL.md`. Verification history: `AUDIT.md`.

## OpenAI pricing detail (GPT-6 Astra, Sol, Luna + GPT-5.6)

**GPT-6 Sol / Luna** (`gpt-6-sol`, `gpt-6-luna`, released 2026-09-22; developers.openai.com model pages and learn.chatgpt.com pricing/models pages, verified 2026-09-22). Sol $2 in / $0.20 cache read / $2.50 cache write / $10 out; Luna $0.10 / $0.01 / $0.125 / $0.50. Both 1.05M context (922K max input, 128K max output; the Codex catalog exposes 272K, max 872K), >272K input reprices the whole request 2× input and cache, 1.5× output. Cutoffs: sol 2026-04-20, luna 2026-05-18. Credits per Mtok (input / cached / output): sol-6 50 / 5 / 250, luna-6 2.5 / 0.25 / 12.5, half the 5.6 rates, which are unchanged. Plus local messages per 5h (estimates): sol-6 15–150, luna-6 350–3,000. Luna-6 efforts stop at `max` (no `ultra`). Codex cloud chats still force gpt-5.6-sol and the cloud model can't be changed; luna-6 is marked not in Codex cloud; sol-6's cloud row is absent. No 5.6 deprecation is scheduled (only gpt-5.5 retires, 2026-10-14); the local catalog points the 5.6 sol/terra upgrade at gpt-6-sol and 5.6 luna at gpt-6-luna. The launch post (openai.com/index/introducing-gpt-6-sol-and-luna/) returns 403 to every fetcher here, so its benchmark table is unverified.

**GPT-6 Astra** (`gpt-6-astra`, released 2026-09-03; official model + pricing pages verified 2026-09-04, API changelog re-checked 2026-09-09 — no price change): $10 in / $1 cache read / $12.50 cache write / $50 out per Mtok; above 272K input the *whole request* bills 2× input and cache, 1.5× output ($20 / $75); Batch and Flex 50%; Fast 2× price ("2× speed, increased usage"). 1.05M context (922K max input, 128K max output; the CLI exposes 272K, max 872K), cutoff 2026-04-30, API efforts `low`…`max` only. Single variant — no mini/nano/pro (GPT-6 sol and luna shipped separately on 2026-09-22). Not offered in Codex cloud (sol is). ChatGPT quota per 5h window: astra 5–45 messages on Plus and Standard Business, 25–225 Pro 5×, 100–900 Pro 20× — vs sol 10–100, terra 25–200, luna 250–2,000 on Plus: roughly ½ sol and 1/50 luna per message, which is the number that governs a subscription-pool fleet. Capability: Anthropic's 2026-09-01 table has Fable 5.1 at 55.8% Terminal-Bench 4.0 vs sol 37.3%; the only astra number (57.9%, secondhand from press) is unverified — pair astra with Fable 5.1 on price, not benchmark. Independent: ARC-AGI-3 99.9% at `high` (arcprize.org, 2026-09-03), Artificial Analysis index 61 (#8 of 201). Vendor system card: astra followed 0% of injected message-board instructions vs sol 52% — and OpenAI states its evals do not cover agents of the same user talking to each other inside one Codex harness, which it observed internally: keep worker workspaces isolated.

**GPT-5.6.** Since the 2026-07-30 cuts: luna −80% to $0.20/$1.20 per Mtok, terra −20% to $2/$12. **Subscription metering (learn.chatgpt.com/docs/pricing, verified 2026-09-10):** Codex usage is per-token credits, one credit = $0.04, rates equal API list price exactly — astra 250 / 25 / 1,250, sol 100 / 10 / 500, terra 50 / 5 / 300, luna 5 / 0.5 / 30 credits per Mtok (input / cached / output); reasoning tokens bill as output; no flat per-message charge, so any API price change passes straight through to quota. The Plus "messages per 5h" figures (astra 5–45, sol 10–100, terra 25–200, luna 250–2,000) are estimates derived from those rates, not fixed limits. Cloud chats force sol and draw more allowance than local messages; `codex exec` is a local message on the same pool. Weekly limits exist but are unpublished. The Codex config reference lists `model_reasoning_effort` only up to `xhigh`; `max` is verified live on the CLI (0.153.4 pin) — trust the CLI, note the doc lag. Sol: $5/$30 on 07-30, cut 2026-08-21 to **$4/$20 (cache-read $0.40), promotional through at least 2026-11-21** — official pricing page, verified 2026-08-25 (aggregator tables and some model cards lagged; trust the CLI source and canonical table). Long-context surcharge (>272K input, whole request): sol $8/$30, terra $4/$18, luna $0.40/$1.80. Family GA 2026-07-09.

**Current anchors (2026-09-22; supersedes the parity notes in the next paragraph):** **Opus 5.5 $4 / $20, cache read $0.20, cache write $5, fast mode $8 / $40** (anthropic.com/claude-opus-5-5 and the platform pricing page, verified 2026-09-22), against Opus 5 $5 / $25 / $0.50. Opus 5.5 is below Fable 5.1 on every line, cache reads included ($0.20 vs $0.25), and scores above it on Terminal-Bench 4.0, CursorBench 4.0 and the AA index (Opus 5.5 evidence below), so **Opus 5.5 replaced Fable 5.1 as the orchestrator lead (user decision 2026-09-22)**. Astra's $1 cache read is now 5× the lead's. Cross-family price parity: sol-6 ($2 / $10) = sonnet 5; Opus 5.5 ($4 / $20) = the gpt-5.6-sol promo; luna-6 ($0.10 / $0.50) is half 5.6 luna. A self-`fork` from an Opus 5.5 session rereads the prefix at $0.20/Mtok (2× cold luna-6 input) and bills output at $20: the old read-and-report caution was priced on Fable's $50 output, and a forked write leg now costs the same per output token as an opus implementer. The sol promo lapse after 2026-11-21 no longer reprices any rung.

**Claude-side anchors (official pricing page, verified 2026-09-01, re-verified unchanged 2026-09-09; historical):** sonnet 5 **$2/$10 — the launch "intro" price was made permanent; the scheduled 2026-09-01 rise to $3/$15 was cancelled**; opus 5 $5/$25; fable 5 $10/$50; **fable 5.1 $10/$50 with cache reads at 0.025× ($0.25/Mtok)** — every other model reads at 0.1×. (Haiku 4.5 $1/$5 for completeness — banned from the fleet, user policy 2026-08-20.) Cross-family (re-verified 2026-09-04, Claude prices unchanged): sonnet-5 ($2/$10) ≈ terra ($2/$12) on price only (no shared benchmark); opus-5 ≈ sol on price and benchmark (opus 5 beats sol on all four shared rows in Anthropic's table); fable-5.1 ≈ astra on list price ($10/$50), but astra's cache read is $1 vs fable 5.1's $0.25; luna @ max owns the cheap context-free tier, while sonnet cache-read ($0.20/Mtok) keeps cache-warm Claude scouts competitive on context-heavy legs. Lead choice, fork economics, and reprice triggers: the current anchors above.

## Fast mode

Fast mode needs **both** `-c service_tier=fast` **and** `--enable fast_mode`, is ChatGPT-auth only, and burns credits at 2.5× (measured on the prior generation). API side: Fast mode replaced Priority Processing 2026-07-30 — sol only, 2× price for up to 2.5× speed — irrelevant for background workers, whose latency the orchestrator absorbs.

## Profiles vs pinned flags

The dispatch template pins flags explicitly so the skill is self-contained regardless of `~/.codex/config.toml`. Profiles — `-p <name>` layering `~/.codex/<name>.config.toml` — can replace the pins, but codex hard-errors if legacy `[profiles.*]` tables remain in `config.toml`; explicit flags are more portable.

## Reaching GPT models from inside Workflow scripts

Workflow scripts' `model:`/`agentType:` params only take Claude values. Reach astra or gpt-5.6 by wrapping: spawn a cheap Claude agent (`model: 'sonnet'`, `effort: 'low'`) instructed to compose the self-contained spec, run `codex exec` per `SKILL.md`, verify, and return the last-message file's content verbatim. A Workflow resume whose run journal is missing fails with a clear error; in auto mode, `agent()` calls with large output schemas go through the classifier.

## codex mcp-server (deprecated 2026-08-24)

`codex mcp-server` (the `codex`/`codex-reply` MCP tools) is deprecated: OpenAI's Codex changelog (2026-08-24) and the docs page say to use the Codex app server, or `codex-plugin-cc` from inside Claude Code, instead. 0.153.4 still ships the subcommand with no deprecation notice in `--help`. No loss for this skill — background `codex exec` keeps the kill/steer control it relies on; build no new wiring on it.

## Native Codex multi-agent (v2 stable as of 0.145.0)

Codex can spawn its own sub-agent threads; settings unified under `[agents]` in config.toml (verified against 0.145.0 source: `enabled` default true, an enabled `features.multi_agent_v2` takes precedence; `max_concurrent_threads_per_session` alias `max_threads`; `default_subagent_model` / `default_subagent_reasoning_effort`; per-role `[agents.<role>]` tables; `max_depth` is V1-only). This skill deliberately doesn't orchestrate through it — separate dispatches keep per-worker kill/steer, and that's structural, not preference: V2 subagents live behind parent-owned mailboxes, and the app-server *rejects* external input to a spawned child (openai/codex#27173) — an outside orchestrator physically cannot steer inside a worker's fan-out. A worker fanning out internally is fine; you verify the final diff. `spawn_agents_on_csv` was **removed** in 0.145.0 (#34413; `job_max_runtime_seconds` survives as a config no-op) — official docs still describe it, ignore them. As of 2026-08-13 the docs match the `[agents]` keys and ship three built-in roles (`default`, `worker`, `explorer`); MAv2 delegation policy is `none` / `explicitRequestOnly` (new-thread default) / `proactive`, with `ultra` as the documented proactive trigger (SKILL.md Notes). "Leaf model" work (#36892) points at per-subagent model assignment — the steering objection stands; re-evaluate on next audit. (0.146.0: agent-plugin manifests + marketplaces, incl. a Claude Code-format one — changes nothing about dispatch mechanics.)

## Subagent role frontmatter (`.claude/agents/*.md`)

Beyond `tools`/`model`/`effort`: `disallowedTools` (denylist, applied before `tools`; both accept `mcp__<server>` patterns), `permissionMode` (**not used, because it's ignored here**: since 2.1.267 a subagent runs in the parent's mode when the parent is in `bypassPermissions`, `acceptEdits`, or auto, and one that declares `bypassPermissions` keeps the parent's mode otherwise. With `defaultMode: bypassPermissions` in user settings, workers run bypass. The roles carried `bypassPermissions` from 2026-09-01 until 2026-09-24; write safety comes from `tools`/`disallowedTools` and the read-only roles' guard hook), `omitClaudeMd` (2.1.271; not used, because workers need CLAUDE.md's style, user-agent, and sourcing rules; each role body instead assigns its approval and PR rules to the lead), `maxTurns`, `skills` (preload full skill content at startup, no `Skill` tool needed; the researchers preload frontier-search), `mcpServers` (scope an MCP server to the worker so its tool schemas never touch main context — inline defs connect only for the worker's lifetime), `memory: user|project|local` (persistent cross-session agent memory), `hooks` (e.g. a `PreToolUse` validator as a hard constraint where prose briefs are soft), `background`, `isolation: worktree`, `experimental.cacheTtl: "5m"|"1h"` (2.1.248 — per-agent prompt-cache TTL, used when no `subagentPromptCacheTtl` setting is configured). A `maxEffortLevel` setting (top-level or per model under `modelSettings`) caps what any role can request.

## Standing roles (portability bootstrap)

SKILL.md routes to standing roles in `~/.claude/agents/`. The role files ship in this skill's `agents/` folder — install with `cp agents/*.md ~/.claude/agents/`, then dispatch **next turn** (a new role file registers only at the turn boundary; so do edits: on 2026-09-24 a same-turn dispatch of an edited role ran its old body). On an update, compare shipped and installed copies, preserve unrelated local changes, and sync only the changed roles; editing `agents/` alone does not update the installed copies. A running session may retain previously loaded instructions, so do not assume an edit was applied to work already in flight. Roles exist because effort is frontmatter-only; the baked-in contracts are convenience. (Project roles live in that project's `.claude/agents/` and are not shipped here.)

Shipped roles and what differs between siblings: `scout-high` (sonnet/high, read-only tools plus an exact-name allowlist of Linear, Google Drive, and Claude Docs read tools; server-level `mcp__<server>` patterns would also grant their write tools; was `scout-low` at sonnet/low until 2026-09-22); `implementer-opus-med` / `-opus-high` / `-opus-xhigh` (one body, `effort` and the tier words in `description` differ; `implementer-sonnet-high` and `implementer-opus-low` retired 2026-09-22 — delete an installed copy if one survives); `pr-reviewer-high` / `-xhigh` / `-max` (one body, `effort` and the routing clause in `description` differ; `-max` carries the user-gated clause); `researcher-opus-med` / `-opus-high` (one body, `effort` and the routing clause in `description` differ; read-only, no Write tool, reports via Bash to the brief-named path, preload frontier-search); `docs-sync-sonnet-high` (sonnet/high, all tools except `Agent` and `NotebookEdit`, edits only brief-named doc paths). Frontmatter surface used: `name`, `description`, `model`, `effort`, `tools`, `disallowedTools`, `skills`, `hooks`.

Role bodies (2026-09-24) separate **fixed rules**, which a brief can't relax, from **defaults**, which the brief overrides: scope, method, source window, return shape, report path. Every body also says CLAUDE.md's approval, planning, and PR-landing rules belong to the lead. The read-only roles (scout, researcher, reviewer) run `~/.claude/hooks/role-guard.py` as a frontmatter `PreToolUse(Bash)` hook. It denies git and GitHub mutations and reuses `destructive-guard.py`'s lexer, so quoted or heredoc text never trips it. Fixtures are in `test_role_guard.py`, run with `python3 test_role_guard.py` in that folder. Neither hook ships with this skill. The command ends in `|| true`, so on a machine without them the roles run with only their text rules. In a live probe (2026-09-24), a scout asked by a brief to commit attempted the commit and the hook blocked it, so the hook is what enforces the floor. Claude Code also injects a subagent note: "Do NOT Write report/summary/findings/analysis .md files … the parent agent reads your text output". Every role that writes a report says this note doesn't cover its report, because the lead reads that file.

## Cross-session messaging fine print

Windows support landed 2.1.241. Loops self-terminate (rate-limit, dedupe, 50/100 caps). The hold-then-drop path: an unset-`crossSessionInbound` bypass receiver *holds* messages from non-bypass senders, then drops after `dialogExpiry` (default 5 min); interactive sessions set accept/hold/refuse and expiry via `/config`. Since 2.1.238 a `refuse` receiver and rate-limit/full-inbox drops report back to the sender — hold-then-expire is the only path still failing silently. `SendMessage` delivers to a bare name matching one live session (`name-word-word` variants on collision); oversized and rapid-burst messages are refused up front.

Same-machine delivery rides a per-session unix socket (`/status` → `Peer address`); each session exports `CLAUDE_CODE_MESSAGING_SOCKET`/`_TOKEN` to hooks and Bash, so a script can post into its own session — a push upgrade over polling progress files (first auth line: `{"type":"auth","token":...}`). Cross-machine and cloud routes go through Anthropic servers via Remote Control; containers can't reach host sessions. `dialogExpiry: "never"` keeps default-held messages until session end. Agent-team teammates are a separate roster, never listed by `ListAgents`; structured team protocol messages stay within the team.

## Agent teams mechanics

(`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`): peer Claude sessions with a shared dependency-tracked task list, inter-agent messaging, plan-approval gates, and quality hooks (`TeammateIdle`, `TaskCreated`, `TaskCompleted` — exit 2 sends feedback and keeps the teammate working). Enabled in user settings 2026-08-02. Verified against docs/changelog: since v2.1.178 every session is one implicit team — spawn teammates via the Agent tool's `name` param, message by name (`team_name` ignored; `TeamCreate`/`TeamDelete` removed — guides describing them are stale). Subagent definitions double as teammate roles (`tools`/`model` honored; `skills`/`mcpServers` ignored). Since v2.1.234 teammates inherit the lead's model and effort unless the spawn names one or `CLAUDE_CODE_SUBAGENT_MODEL` is set. Since 2.1.265 teammates and resumed subagents keep SubagentStart hook context and preloaded skills inside the cached prefix on later turns (a cache-miss class before). **Naming trap:** any named Agent spawn launches as a teammate — even self-named during ordinary delegation. Its final answer rides in the idle notification, but it is still a teammate (no auto worktree, team-lifecycle semantics, top cost surface); set the env var to `0` (hot-applied) to get named subagents back. Display default is `in-process` since v2.1.179; split panes need tmux or iTerm2+`it2` (not VS Code terminal/Windows Terminal/Ghostty). Cost: official estimate ~7× a standard session with plan-mode teammates — Sonnet teammates, 3-5 of them, ~5-6 tasks each, shut down when done. Limits: no `/resume`/`/rewind` of in-process teammates (lead may message dead teammates after resume), no nested teams, no background subagents from an in-process teammate, task-status can lag, one lead/team per session, no automatic worktree isolation — partition by file ownership; task-claim locks don't guard file edits. On disk (docs 2026-08-13): mailboxes `~/.claude/teams/{team}/inboxes/{agent}.json`, roster `~/.claude/teams/{team}/config.json` (teammates can read it to discover peers — brief them to), tasks `~/.claude/tasks/{team}/` — the task list survives resume; in-process teammates don't. Teammates never appear in `ListAgents`: team roster and cross-session messaging are separate namespaces sharing one `SendMessage` tool (cross-session is plain text only). Org kill switch: managed settings denying `SendMessage`/`ListAgents` also remove subagent and teammate messaging — SendMessage-resume can be silently disarmed in a managed org.

## Watch items (not adopted)

- **Astra-as-worker / lane-lead A/B (added 2026-09-04):** lane-lead and plan-critic rows entered `SKILL.md` on 2026-09-04 as A/B-pending: the first real multi-subsystem lane runs paired (astra lane lead vs sol `high` under your supervision; sol-6 since 2026-09-22), defects counted by the cross-model reviewer; drop the tag or the row on the result. Original note: the only astra-vs-Fable-5.1 coding number is secondhand and inside harness noise. Before promoting astra above sol for implementer legs, run one paired dispatch on a real silent-failure-domain task and compare defects found by the cross-model reviewer. Re-check when OpenAI's announcement page (403 to fetches) or an independent leaderboard publishes SWE-bench/Terminal-Bench for astra.
- **`gpt-reserve` / "Luna Reserve"** (hidden catalog entry, 2026-09): post-exhaustion overflow entitlement, no API price, CLI can't select it (openai/codex#40939, #41520, #42830). Not a routing target; note only that a quota-walled lane may silently continue on it.
- **Server-driven model catalog:** `~/.codex/models_cache.json` is etag-refreshed from the server and decides the bare default model (astra promoted to `list`/priority 1 on 2026-09-04; 0.153.4 made astra the *bundled* default too, so both catalogs now agree). Pinning `-m` on every dispatch is the defence; `codex doctor --json` shows the resolved default.
- **Astra availability on the Plus tier (added 2026-09-09):** OpenAI's help centre says Plus gets astra "in ChatGPT Work and Codex as it rolls out"; a 2026-09-05 community report saw only 5.6 models inside Codex on Plus, with no staff reply. Probe before a session's first astra lane (SKILL.md preflight). Help-centre pages return 403 to every fetcher here — verify in a browser.
- **Work/Codex rate card vs API page (added 2026-09-09, access-limited):** a cross-model sweep reported that the ChatGPT rate card (help.openai.com article 20001415) lists no >272K multiplier and no cache-write charge for Codex astra, with fast mode at 2.5×. If true, the 272K chunking rule is quality advice rather than a cost rule on the subscription pool. Unverified here (403); check by hand before changing the rule.
- **Astra `low` rung calibration (added 2026-09-10):** the review-caught default rests on Terminal-Bench 4.0, Zapier and the AA index — astra has no effort-resolved DeepSWE row and no ReactBench row. Log, per real leg for a few weeks: which rung was dispatched, whether it passed first try, and which rung rescued a failure (rescue rate of astra low over terra xhigh, of astra high over astra low). Re-check benchmarklist DeepSWE and reactbench.com for astra rows on the next audit. If astra low's first-try rate on review-caught repo legs falls below terra xhigh's DeepSWE 60%, move the review-caught default to astra `high` rather than back to sol.
- **Claude-side rungs (verified 2026-09-10, re-based on Opus 5.5 2026-09-22):** CursorBench moved to 4.0 on 2026-09-10 and dropped the 3.2 rows the 09-10 ladder rested on; the Opus 5.5 evidence section below is the current basis. Open: **Arena Agent Work and WebDev have no Opus 5.5 rows** (as of 2026-09-22) — that is the steerability evidence behind both the Opus 5.5 lead decision and `pr-reviewer-high` as default, and the WebDev basis for `pr-reviewer-max`; re-check when Arena adds the model. **tbench.ai has no Opus 5.5 row**, so its Terminal-Bench figures are Anthropic's alone, and Anthropic's chart disagrees with the live board on Opus 5 and Fable 5.1 by 2–3 points. **Medium floor is on trial (user, 2026-09-22):** record first-try acceptance for `implementer-opus-med` on machine- and review-caught legs; if those legs pass first try nearly every time, `low` would likely have passed too, and the ≈ 2× cost-per-success gap argues for trying a `low` rung again — raise it with the user, do not restore it unasked. Opus 5.5 `xhigh` over `high` is unmeasured on real legs; if a near-miss rescue by `-xhigh` recurs, reconsider it as a rung. **Plan metering:** no Anthropic doc gives a formula for subagent usage against Pro/Max limits. Documented (code.claude.com/docs/en/costs, support.claude.com/en/articles/11145838): limits are shared across Claude and Claude Code, cost is token-based and scales with context, `/usage` attributes usage to subagents separately, cache reads bill at ~10% of the input rate, and subagents sit in the five-minute cache TTL bucket even on a subscription. So the opus `low`/`medium` token advantage (75–80% fewer tokens than sonnet `max`) is the operative quantity for plan headroom, with the exact weighting unknown.
- **Sol-6 unmeasured on repo-shaped boards (added 2026-09-22):** the sol-6 `high` rung rests on the AA index, AA's own Terminal-Bench 4.0 runs, and price. tbench.ai, DeepSWE 1.1, ReactBench and SWE-bench Pro had no gpt-6-sol or gpt-6-luna rows on 2026-09-22; secondhand launch coverage (VentureBeat) claims DeepSWE sol-6 max 68.8% and luna 66.6% — unverified. Re-check those boards on the next audit and compare sol-6 `high` against 5.6 sol `medium`'s DeepSWE 61.06% / $1.86. `node_repl_auto_review_required: true` is set on both sol-6 and astra (luna-6: false) in the 0.155.1 catalog. Read-only probe 2026-09-22 (`-m gpt-6-sol -c model_reasoning_effort=high -s read-only --ephemeral`, one turn): shell command and Node REPL `js` call both completed with no approval, auto-review, or denial reported; 94.3K input (81.4K cached), 712 output, ≈ $0.05. Still open: record what the flag does on the first sol-6 bypass dispatch.
- **Retired 2026-09-22 — sol promo expiry:** no rung runs gpt-5.6-sol any more, so the 2026-11-21 promo lapse only reprices Codex cloud chats, which have no engine-table row.
- **Astra mid-turn controls (API changelog 2026-09-03):** async tool calling, mid-turn steering over WebSockets, and changing reasoning effort mid-conversation landed on the API. Not exposed by `codex exec`/`queue` as of 0.153.4; re-check when a stable 0.154 ships — it would change the directing table.
- **Headless `-p --resume` by exact session id** failed once (2026-09-09, claude-code 2.1.266, `No conversation found with session ID`) after the headless review had exited 0; cause not established. 2.1.267 fixed two unrelated `-p --resume` bugs. Until reproduced, seed a fresh `-p` from the artifact instead of debugging the resume.

- **Luna long-context integration cliff (added 2026-09-09, developer-reported, unverified):** a 2026-09-09 GPT-5.6 capability-routing report (local, not in the repo) cites OpenAI's GPT-5.6 launch page for MRCR v2 8-needle: sol 91.5% / 73.8%, terra 89.6% / 72.5%, luna 41.3% at both 256K–512K and 512K–1M (the repeated figure may be a transcription error); GraphWalks BFS 1M F1 sol 77.1, terra 71.2, luna 51.2. The page serves a JS shell to WebFetch and curl, so the numbers are unconfirmed. Encoded as a *provisional* ~200K integration gate in the Luna latitude test; confirm against the page or an independent long-context eval, then harden the clause or drop it. **Carried to luna-6 unmeasured (2026-09-22):** no MRCR or GraphWalks row exists for it; AA-LCR at `max` is 0.833 vs 5.6 luna 0.837, a reasoning benchmark, not an integration one.
- **Codex `tools.web_search` object filtering (moved from SKILL.md 2026-09-09):** the object form (`context_size`, `allowed_domains`, `location`) parses under `--strict-config` on 0.153.4, but whether `allowed_domains` actually filters results is unprobed. Live-verify with a read-only lookup whose answer only exists on an excluded domain before routing a domain-scoped leg to Codex; on a pass, drop the Notes caveat in `SKILL.md`.
- **`openai/codex-plugin-cc`** (official OpenAI plugin, v1.0.6 2026-07-08 — still latest 2026-09-09, and now OpenAI's named replacement for `codex mcp-server` inside Claude Code; ~29k stars) runs Codex inside Claude Code — `/codex:review`, `/codex:adversarial-review`, `/codex:rescue`, `/codex:transfer`, background jobs, optional Stop-hook review gate; wraps the local codex binary (not MCP). Not adopted: background `codex exec` keeps kill/steer control. Evaluate on next audit; OpenAI warns its auto review loop can drain usage limits.
- **Remote lanes (re-checked 2026-08-13; not usable-verified — no engine-table row):** `codex cloud exec` is documented (`--env` pins the environment, `--attempts 1–4`; `list` scriptable) — docs-verified only, never run end-to-end; multi-attempt only makes sense with an independent judge ranking outputs. The Agent tool's `isolation: "remote"` is gated and **falls back to local silently** (probe identity: same hostname/user/cwd — the fallback looks exactly like success). `claude --cloud` sessions start from the *remote* branch — unpushed local state never reaches them. No docker here — per-worker ports/temp/DB names (Worker pools) remain the tool.
- **Orchestrator bridge prototype (built 2026-09-11; pilot 1 done same day on a project task, clean first run plus one corrective round, nine adapter gaps logged in its README):** `~/.codex/orchestrator-bridge/` hosts a persisted `gpt-6-astra` lead through the Codex Python SDK (0.154.0 pinned with codex-cli 0.154.0) and moves the task record, second-rejection checkpoint, send-back cap, dedup, stale-SHA marking, and run numbering out of prose into an adapter; results enter as `ExternalMessage` tool output. It is Codex-lead only; this skill's Claude-led flow and its counters in `references/briefs-and-repairs.md` are unchanged. If the pilot holds (measures: handles lost, checks run twice, checkpoint timing), decide whether the same adapter should front Claude-led runs. Design evidence and SDK caveats: the Codex skill's `references/external-message-watchlist.md`.
- **Codex native `--worktree` (probed 2026-09-11 on 0.154.0, not adopted):** needs `--enable worktrees`; creates `~/.codex/worktrees/<4-hex>/<repo>` at a **detached HEAD** (a worker's commits land on no branch), persists after the turn, and leaves the main tree untouched. `exec resume` runs in the invoking cwd, not the managed tree — you must `cd` into that path yourself; `resume --worktree` is refused ("use `codex exec fork --worktree`"), which creates a *new* tree. Manual `git worktree add -b <task>` plus the helper's `workspace` stays the rule; re-probe if a later CLI adds a branch option or thread-bound cwd.
- **`/batch` packaged skill** splits one change into 5–30 worktree-isolated subagents, each opening a PR — in direct tension with this skill's 3–5 concurrent cap (user policy). Not adopted; if a job wants that scale, raise it with the user rather than silently exceeding the cap.
- **A2A protocol 1.0** landed under the Linux Foundation (150+ orgs; signed Agent Cards, multi-tenancy, multiple bindings). Neither Claude Code nor Codex speaks it — no action, re-check on next audit.
- **Judge-reliability corpus** (RuVerBench arXiv:2606.29920; Judge Reliability Harness arXiv:2603.05399; "Reliability without Validity" arXiv:2606.19544): no judge uniformly reliable; agreement ≠ validity. Also candidate pattern sources not yet folded in: SWE-RPG (arXiv:2608.09072 — spec failure as first-order fleet risk), SpecBench (arXiv:2605.21384 — visible-test pass ≠ done; hidden-validation gap grows with codebase size), harness-cost work (arXiv:2607.06906 — ~41% cost recoverable via harness design), per-step routing (arXiv:2606.27457).

## Review and repair evidence (added 2026-09-09)

Why send-backs carry a diagnosed defect, why reviewers get artifacts rather than transcripts, and why the repair count follows the task:

- **Repair or Resample? (arXiv:2608.25920, 2026-08-26):** 536 annotated multi-agent failure trajectories; unguided reruns reproduced 67.97% of failures and repaired 6.90%, symptom-driven intervention repaired 20.15%. A re-dispatch without a diagnosed defect is a resample, not a repair — the directing table's send-back row and the per-task repair count rest on this.
- **The Interaction Tax (ICML 2026, arXiv:2608.23541, 2026-08-24):** when agents read each other's complete outputs, proposals converge within one round; "multi-agent performance depends less on the number of agents than on the information they exchange." Reviewers get the diff and the author's report as leads, never the author's verdict or transcript; scouts feed implementers by artifact path.
- **Adversarial Review (arXiv:2608.18167, 2026-08-16):** a reviewer plus a critic that audits the review beat a five-agent baseline on LiveCodeBench with three agents; the naive protocol hit a false-consensus mode on SWE-PRBench until the prompt demanded explicit, evidence-grounded disagreement. Evidence for "findings are claims, not verdicts" and CONFIRMED/PLAUSIBLE labelling — not, on its own, grounds for a standing critic role.
- **Anthropic, Patterns and problems in multiagent systems (2026-08-13):** 18 of 30 agents chose the identical branch name; a job-queue fleet polling at 30 Hz issued 2.4M requests and accepted 117 jobs. The lead assigns branch and worktree names; workers never poll.
- **Codex-session exercise (2026-09-09, validation report kept locally):** a successor worker silently adopted a checkpoint's task id that did not match its brief; a status-file write was refused under model-reviewed auto-approval and a read-only CHECKPOINT turn was the working fallback. Source of the identity-mismatch rule, the repair-count column, and the `--approve-for-me` progress-file caveat. Unverified lead from the same pass: a Feb 2026 paper on consensus-over-expertise losses (up to 37.6%) surfaced only in a search snippet.

- **Send-back cap (2026-09-10, local overnight Codex usage report):** a terra `max` implementer on a project task was auto-resumed through eight turns and four rejected review checkpoints, 10:44 p.m. to 7:01 a.m., 1,399 responses and 801k output tokens (30% of all local Codex output that night). In its solo window, 12:49 to 5:49 a.m., the weekly meter moved three points on ~520k output tokens, so the whole session cost roughly 4–5 of the 21 points lost overnight; the rest was evening concurrency across five projects. The orchestrator never surfaced the repeated rejections. Hence the default directing-table checkpoint at a second rejected review. An explicit applicable user/project operating agreement can supersede that default; record the source and resolved policy as described in [briefs and repairs](references/briefs-and-repairs.md#task-record-and-resume-check).

## GPT-6 Sol/Luna and Opus 5.5 routing evidence (added 2026-09-22)

Two researcher-opus-med passes on launch day, every figure at its primary; secondhand press excluded. Decisions taken 2026-09-22 (user): Codex ladder luna-6 `max` → sol-6 `high` → astra `low` → `high` → `xhigh`, no 5.6 tier on it; Claude ladder opus 5.5 `low` → `medium` → `high`, `xhigh` near-miss only, `implementer-sonnet-high` retired, sonnet kept for `scout-low` and docs sync; `pr-reviewer-high` is default and judge; Opus 5.5 is the orchestrator lead. Revised the same day (user): `implementer-opus-low` retired and opus `medium` is the floor for every Claude coding leg, multi-subsystem and silent-failure legs included, to be judged on live runs; sonnet moved to `high` for recon (`scout-high`) and docs sync. The figures below favour `low` on cost per success (≈ $2.68 vs ≈ $5.54 on CursorBench 4.0); the floor is a user decision, not a reading of them.

- **CursorBench 4.0 (cursor.com/evals, updated 2026-09-10 with new long-horizon problems; the same rows appear in Anthropic's launch post, so one source, not two):** low / medium / high / xhigh / max — Opus 5.5 43.7% $1.17 (15.8K tokens, 28 steps) / 52.5% $2.91 (38.0K, 54) / 56.0% $3.97 (53.1K, 68) / 56.0% $6.98 (101K, 109) / 57.8% $13.43 (218K, 185); Opus 5 40.7% $4.87 / 43.3% $6.94 / 44.7% $9.00 / 46.1% $11.43 / 46.6% $11.95; Sonnet 5 24.1% $1.39 / 28.0% $2.31 / 30.8% $3.48 / 32.0% $4.55 / 34.1% $7.17; Fable 5.1 low 45.1% $5.44, max 51.8% $17.28. Opus 5.5 `low` beats every Sonnet 5 rung and costs less than Sonnet `low`; cost per success (cost ÷ pass rate) opus 5.5 low ≈ $2.68, medium ≈ $5.54, sonnet high ≈ $11.30. `xhigh` ties `high` at +76% cost — the basis for dropping `xhigh` as a rung.
- **Anthropic launch post (anthropic.com/claude-opus-5-5, 2026-09-22; vendor):** Terminal-Bench 4.0 per effort, Opus 5.5 38.5% $1.29 / 57.6% $2.94 / 64.2% $3.88 / 66.4% $7.35 / 64.8% $11.24 vs Opus 5 28.5% $4.25 / 41.2% $7 / 47.0% $10.64 / 50.6% $13.48 / 52.3% $15.83, Fable 5.1 max 55.8% $19.50. FrontierCode v1.1 Opus 5.5 47.3% / 54.6% / 54.0% / 51.4% / 54.4% (non-monotonic: `xhigh` below `medium`). Default effort `medium` "beats Opus 5 at max effort for about a fifth of the cost". Vendor-curated customer quotes (Deloitte: `low` caught 72% of known bugs vs Opus 5 `high` 56%) are early-signal only.
- **Artificial Analysis (artificialanalysis.ai/models/claude-opus-5-5; per-effort values from embedded page data, headline values match the rendered page):** index (total cost to run) Opus 5.5 low 42.3 ($860) / medium 51.2 ($1,627) / high 53.6 ($2,172) / xhigh 56.0 ($4,057) / max 57.6 ($8,708); Opus 5 39.4 / 44.8 / 48.1 / 49.7 / 50.8 ($1,561 → $7,275); Sonnet 5 high 31.7 ($2,086), max 38.2; Fable 5.1 max 53.4 ($13,129). No per-task cost for the effort variants.
- **Terminal-Bench 4.0 live board (tbench.ai):** no Opus 5.5, sol-6 or luna-6 rows on 2026-09-22. Opus 5 rows now exist (Claude Code harness, 2026-07-24, n = 330): low 34.85%, medium 44.85%, high 50.3%, xhigh 53.94%, max 51.82%. The board has Fable 5.1 max 57.88% where Anthropic's table says 55.8%.
- **Artificial Analysis v4.3.2, Codex tiers (index / cost per task / output tokens / AA's own Terminal-Bench 4.0 run):** luna-6 max 37.26 / $0.07 / 150M / 12.6%; 5.6 luna max 37.32 / $0.18 / 150M / 11.6%; sol-6 low 33.9 / $0.13; sol-6 medium 39.8 / $0.25 / 16M / 18.7%; 5.6 sol medium 39.2 / $0.50 / 21M / 14.6%; **sol-6 high 42.8 / $0.37 / 25M / 26.3%**; sol-6 xhigh 44.1 / $0.53 / 30.3%; sol-6 max 47.5 / $1.06 / 43.9%; astra low 45.8 / $0.82 / 10M / 41.9%. Sol-6 `high` scores above the old rung for less money, and the user chose it over sol-6 `medium` (the measured gap between them: index 42.8 vs 39.8, AA Terminal-Bench 26.3% vs 18.7%, for +48% cost per task). Sol-6 `max` edges astra `low` on the index but costs 1.3× and, per the system card, takes injected actions (next bullet), so astra `low` keeps the review-caught rung.
- **GPT-6 Astra system card, Sol/Luna appendix (deploymentsafety.openai.com/gpt-6-astra, §11, added 2026-09-22):** indirect prompt-injection robustness sol-6 99.05%, luna-6 98.61%, astra 99.79%, GPT-5.6 96.23%. Multi-agent message board: 5.6 sol engaged 84% / took the unauthorized action 52%; astra 27% / 0%; **sol-6 26% / 11%**; luna-6 0% / 0% but found the board in only 76% of samples. Untrusted-content legs stay on astra `xhigh`.
- **DeepSWE 1.1 (benchmarklist.com, updated 2026-09-03):** no GPT-6 sol/luna rows. It does carry **5.6 luna max 67.19% at $3.03 per attempt**, above 5.6 sol medium's 61.06% / $1.86. Not previously recorded; it supports the latitude test's premise that luna at `max` handles more than mechanical grind, but no rule changed on it.
- **Claude Code (code.claude.com changelog 2.1.280, model-config docs):** `opus` resolves to Opus 5.5 on the Anthropic API from 2.1.280, so every `model: opus` role switched on upgrade with no file change. Opus 5.5 defaults to `medium`, ignores a top-level `effortLevel` in user settings, and thinks more per turn than Opus 5 at the same effort ("most of all at `xhigh` and `max`"). Role frontmatter `effort` overrides the session level.

## Claude tier routing evidence (added 2026-09-10; superseded 2026-09-22 — CursorBench 3.2 rows are gone from the board and the rungs are re-based on Opus 5.5, section above)

Source: a local Claude effort-routing report (snapshot 2026-09-09 ET). **All figures below re-verified at the primaries on 2026-09-10** (retrievals 15:12–15:18 UTC); corrections applied in place. Same method as the Codex ladder: per-effort pass rate, cost, tokens, steps; expected cost to success ≈ cost per attempt ÷ pass rate.

- **CursorBench 3.2 (cursor.com/evals; ambiguous multi-file tasks from real sessions, all ten configurations, one harness):** sonnet low/medium/high/xhigh/max = 47.7/52.4/56.9/58.7/61.5% at $0.87/$1.44/$2.13/$2.77/$4.30 per task; opus low/medium/high/xhigh/max = 62.8/64.3/66.7/69.3/70.0% at $2.55/$3.29/$3.91/$7.35/$8.23. Tokens: sonnet max 92.9K and 86 steps vs opus low 18.5K and 37 steps. Every opus rung beats every sonnet rung; opus low, medium and high all beat sonnet max at lower cost. The high→xhigh step on opus is the compute cliff (+2.6 pts for +88% cost, +94% tokens); xhigh→max buys +0.7 pts. Cursor warns small gaps may be noise.
- **Arena Agent Work (arena.ai/leaderboard/agent/work, 2026-09-08 snapshot, ~879K sessions):** net improvement opus high 11.62 ± 2.00, opus max 10.41 ± 2.28, sonnet high 7.00 ± 2.30; confirmed success 12.64 / 10.76 / 3.83; steerability 11.99 / 9.24 / 8.26; P50 cost $4.07 / $5.48 / $1.47; P50 output 48.0K / 66.6K / 37.2K. Max leads only on bash recovery (12.86 vs 12.10). Pareto frontier holds both opus high and sonnet high. Basis for `pr-reviewer-high` as the default reviewer and `-max` user-gated.
- **Artificial Analysis v4.3:** opus low/medium/high/xhigh/max index 40/45/48/50/51 at $1.10/$2.19/$3.61/$4.88/$5.86 per task; sonnet low/medium/max 25/28/38 (high and xhigh rows incomplete — do not interpolate). Opus low → xhigh gain is domain-specific: AutomationBench 52→53, Terminal-Bench v4 26→46, AA-LCR 81→80.
- **Where max does win:** Arena WebDev opus max ~1688 vs high ~1661 (reference-based design 1712 ± 15 vs 1678 ± 15); ARC-AGI-2 (Epoch) max 90.4% vs high 88.3%. Text Arena overall factuality: high 1505 ± 5, max 1505 ± 6 — a tie, so no factuality edge for max. Hence max as a domain branch, not a rung.
- **Not adopted:** Opus 5 task budgets (`output_config.task_budget`) are API-only — the Agent tool exposes effort through role frontmatter and nothing else. Terminal-Bench 2.1 has sonnet 5 high at 74.6% and no verified Opus 5 row.

## Tier routing evidence (added 2026-09-09)

Source: the 2026-09-09 luna/terra/sol capability-routing report above; five of its six load-bearing figures re-verified against the primaries this run.

- **Vals Valkyrie (updated 2026-08-15, Mini-SWE-agent, 46 repository tasks):** sol 41/46 at $1.44 and 9m14s per test, terra 40/46 at $0.37 and 3m35s, luna 40/46 at $0.07 and 6m23s; sol-only 2 tasks, luna-only 1. Strongest external support for the Luna latitude test (bounded + machine-checked → tier gap nearly vanishes). Sol row verified on the page; luna row was outside the fetched excerpt.
- **Artificial Analysis Intelligence Index v4.3 (verified):** luna none/low/medium/high/xhigh/max = 17/22/26/33/35/38, terra max 42, sol max 47. Monotonic on every tier — supports the `max` pin on luna. The terra pin moved to `xhigh` on 2026-09-10 on cost-to-solution grounds (Seven-way routing evidence below), not on the index. Composite equivalences (luna-high ≈ terra-medium, terra-max ≈ sol-high) are broad-index only; the report's own domain tables break them.
- **Effort non-monotonicity is domain-specific, not adopted as a dial:** WeirdML sol high 88.8% vs max 87.0% (within noise); ToneBench (verified, 2026-09-03 runs, 10 briefs × 5) shows effort flat on prose (luna none 81.6 → xhigh 82.9; sol none 87.3 → high 87.4). Neither is a luna/terra coding result, so effort stays pinned.
- **Diagnosis vs implementation (NetMentor, verified, N = 2 tasks in one repo, sol run at high vs default on the others):** review luna 38 / terra 62 / sol 74 of 100; implementation luna 70 / terra 60 / sol 70. Hypothesis-grade only; consistent with review and repair already living on the astra rungs.
- **Not adopted from the report** (sol-on-the-ladder was later adopted 2026-09-11, see Seven-way routing evidence): research to sol (Search Arena sol-xhigh 1257 ± 7 vs Opus 4.6 Search 1253 ± 5 is a tie against a prior-generation Claude), the 150–300-task × 18-config router benchmark and ten-feature 0–3 vector (the three-question latitude test collapses the same features; the audit log is the calibration record). The report also omits sol's promo pricing end (2026-11-21).

## Seven-way routing evidence (added 2026-09-10; GPT-5.6 rungs superseded 2026-09-22, section above)

Source: two local 2026-09-09 routing reports (terra xhigh/max vs sol medium/high; the same plus astra low/high/xhigh), every load-bearing figure re-verified at the primaries on 2026-09-10 by an opus/medium researcher. Decisions taken 2026-09-10: ladder luna → terra `xhigh` → astra `low` → astra `high` → astra `xhigh`; terra `max` and sol off the ladder; starting rung set by who catches a failure. **Superseded 2026-09-11 (user decision, no new evidence):** ladder is luna → sol `medium` → astra `low` → astra `high` → astra `xhigh`; sol `high` is not a rung; terra off the ladder entirely; React / component legs go to Claude opus `high`.

- **DeepSWE 1.1 (benchmarklist.com, verified mini-SWE-agent runs 2026-08-26, 113 tasks; all four GPT-5.6 rows match to the decimal):** pass@1 / pass@4 / cost per rollout / mean steps / wall time — terra xhigh 60.18% / 80.53% / $2.13 / 43 / 9.7 min; sol medium 61.06% / 80.53% / $1.86 / 31 / 7.1 min; sol high 69.40% / 86.73% / $3.47 / 37 / 9.9 min; terra max 69.62% / 88.50% / $4.95 / 76 / 16.9 min. Expected cost to one accepted solution (1/pass@1 attempts): terra xhigh $3.54 with 0.66 reviewer-facing failures, sol high $5.00 with 0.44, terra max $7.11 with 0.44. Terra max buys completion through persistence (2× steps, 2.5× output of sol high at the same pass@1) — the source of the "resume before tier change" exit and of dropping terra `max` as a default. Astra has a single launch-post row (74.1% vs sol 72.7%) with pass@4, cost, steps and wall time blank — **no effort-resolved astra DeepSWE data exists**; anything claiming otherwise is fabricated.
- **Terminal-Bench 4.0 live board (tbench.ai, Codex agent, n = 330, verified 2026-09-10):** astra max 58.2% ± 2.8 ($3,267); astra xhigh 57.9% ± 2.7 ($2,351); astra high 57.9% ± 3.0 ($2,269); astra medium 54.2% ± 2.7 ($1,915); astra low 50.6% ± 2.8 ($1,557, 890M tokens); sol max 37.3% ± 3.8 ($2,542, 4.4B tokens); terra max 21.5% ± 3.3 ($1,734, 5.7B tokens). Astra low beats terra max by 29 pts and sol max by 13 pts at the lowest cost of the three; high over low is +7 pts for +46% cost; xhigh and max add nothing over high (effort non-monotonic at the top, as before). **The seven-way report's "Terminal-Bench 2.1" table (astra low 86.7% within 0.7 of high, terra max 78.4%) does not exist on the live board** — the URL redirects to 4.0, no 2.1 rows are served, and Artificial Analysis's own 2.1 field has every astra effort at 88–90 (saturated). Do not reuse it.
- **Artificial Analysis v4.3 (verified, 11/14 figures exact):** index terra xhigh 38, sol medium 39, sol high 42, terra max 42, astra low 46, astra high 51, astra xhigh 53. Cost per task terra xhigh $0.63, sol medium $0.50, sol high $0.81, astra low $0.82, terra max $1.40, astra high $1.72. Output tokens per task astra low 4K, sol medium 8K, astra high 12K, sol high 13K, terra xhigh 20K, terra max 39K. Time per task astra low 93 s, astra high 239 s, terra max **455 s** (the report said ~341 s — wrong by a third). Terminal-Bench 4.0 component: terra xhigh 10%, sol medium 15%, sol high 21%, terra max 35%, astra low 42%, astra high 54%. Sol high keeps AA-Briefcase, GDPval, SciCode and long-context (AA-LCR 82 vs astra low 80) — none of which Codex worker legs carry.
- **Subscription-credit view of the same rows** (output credits per AA task = tokens × rate card): astra low ~5.0K, terra xhigh ~6.0K, sol high ~6.5K, terra max ~11.7K. Astra low is the cheapest non-luna rung on the sub's own accounting despite the highest per-token price — the metering finding that made the astra `low` rung affordable.
- **Zapier AutomationBench v1.0.6 (verified):** astra low 30.29% / $1.08, high 37.14% / $1.45, xhigh 38.96% / $1.53, max 41.4% / $1.77. Second confirmation that high buys real completion over low; low still has the best inference-cost-per-success (~$3.57 vs $3.90 vs $3.93).
- **ReactBench (verified, 54 entries, no astra rows):** terra xhigh 37.4% / $0.60, sol medium 32.3% / $1.10, sol high 37.4% / $1.58, terra max 40.5% / $1.13, sol max 46.7% / $3.06. Reverses the DeepSWE ordering — domain matters. Terra `max` retains a React-specific prior; it is not a rung.
- **Sol medium adopted 2026-09-11** as the machine-caught rung; a sol failure moves model to astra `low`, sol high is not a rung (user decision). Known trade-offs carried, not refuted: sol medium loses to terra xhigh on ReactBench (hence React → Claude opus `high`), sol is on promo pricing through 2026-11-21 (watch item), and on coding-shaped work the astra `low` rung still dominates on subscription credits. Still **not adopted:** astra `low` as the universal first rung (no repo-shaped measurement); the reports' 150–300-task router benchmark.

## Context fuse evidence (added 2026-08-25)

Why the fuse is semantic-trigger + orchestrator-choice, not a token cap:

- **Anthropic, effective context engineering (2025):** three long-horizon tools — compaction (preserve decisions/unresolved bugs, drop stale tool output), structured note-taking (artifact survives context resets), sub-agents (explore on tens of thousands of tokens, return 1–2k distilled). Choose by task shape. https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents
- **Anthropic, long-running harnesses (Nov 2025 + Mar 2026 follow-up, via cross-model sweep):** compaction alone insufficient; structured handoff + fresh session beat continued compaction on Sonnet 4.5 ("context anxiety"), while Opus 4.5 ran fine on continuous compaction — reset policy is model/workload-dependent: an orchestrator judgment, not architecture. https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents
- **Handoff Debt (arXiv:2606.02875):** 181 takeover tasks — context-bearing handoffs cut successor agent events 20–59% and prompt tokens 42–63% vs cold takeover; structured/summary notes beat raw traces. Respawn-from-artifact is cheap when the artifact is good.
- **Governance decay (arXiv:2606.22528):** 1,323 episodes, 7 model families — 0% policy violations with constraints in context, ~30% avg (up to 59%) after compaction dropped them; constraint pinning restores 0%. This is why respawn briefs restate the standing clauses and why worker auto-compaction is never trusted to preserve them.
- **Engram (arXiv:2603.21321):** fresh successors with empty context + persistent digest/archive outperform both raw-history transfer and no-handoff; the distilled digest matters more than the raw archive.
- **Cognition, multi-agents that work (Apr 2026):** clean-context reviewers beat context-sharing ones ("context rot"; avg 2 bugs/PR caught); single-writer + intelligence-contributing helpers. https://cognition.com/blog/multi-agents-working
- **Convergent synthesis (both research tracks):** compact within a coherent phase; respawn at phase boundaries, on drift, or for independent review; summaries are navigation aids while artifacts/files stay the authority (Manus: drop content only with a restorable handle). Trigger-policy confidence is moderate — model- and workload-dependent; the architecture (external state + structured handoff + semantic triggers) is the high-confidence part.

## Claude harness fine print (moved from SKILL.md 2026-09-10)

Version-specific and rarely needed Claude-side mechanics; the hot-path rules stay in `SKILL.md` (Claude worker mechanics).

- **Resume and cache:** a per-invocation `model` sticks across `SendMessage` resume. Resume is cache-stable from 2.1.265 (2.1.267 for appended-system-prompt roles and forked background workers; earlier builds re-rendered the prompt prefix on resume). Subagent transcripts: `~/.claude/projects/{project}/{sessionId}/subagents/agent-{agentId}.jsonl`.
- **Cross-session messaging** is on by default: `SendMessage`/`ListAgents` reach independent sessions (background, `claude -p`, other terminals, cloud); plain text only. `notify_when_idle` hears a peer finish without polling. Delivery fine print: the Cross-session messaging section above.
- **Effort and roles:** `effort: low`→`max` in frontmatter overrides session effort on every model; capped by any `maxEffortLevel` setting. `--append-subagent-system-prompt-file` extends every role's prompt at launch. `experimental.cacheTtl: "1h"` in a role file keeps a long-lived role's prefix warm (session-wide: `subagentPromptCacheTtl`). `claude --agents '<json>'` at launch is session-scoped, immediate, and accepts `effort`. Project roles live in the project's `.claude/agents/`.
- **Model resolution:** per-invocation `model` > frontmatter > `CLAUDE_CODE_SUBAGENT_MODEL` (a default, not an override) > inherit; `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` makes the env var win. A *restricted* requested model warns and runs the parent model. `model: fable` roles honour a `[1m]` tag on `ANTHROPIC_DEFAULT_FABLE_MODEL`.
- **Output handling:** `taskOutputMaxChars` (≤128K) sets how much task output arrives inline before spilling to a file (disk copy capped at 1 GB) — raise it for large-diff legs. A mid-stream cut auto-continues; a subagent that `SendMessage`-resumed another agent is woken by its completion; subagent-started background commands have no one-hour cap. `subagent_tokens` lands only at completion (a worker cannot count its own tokens — hence the semantic context fuse).
- **Worktrees:** `worktree.baseRef` accepts `"head"` only, not branch names — provision manually for a branch base. Commands that resolve to the main checkout fail inside a worktree.
- **Fan-out env:** `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` (default 3), `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS` (default 20); no lifetime cap. MCP calls over 2 minutes auto-background. `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1` forces every subagent foreground. `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=0` hot-disables the teammate naming trap.
- **Headless and forks:** `claude -p --bare` for scripts/CI (a `cd` persists across stream-json turns since 2.1.265; an exact-id `-p --resume` failed once unexplained on 2.1.266 — seed a fresh `-p` from the artifact rather than debug it, see watch items). `-p`/SDK forking needs `CLAUDE_CODE_FORK_SUBAGENT=1`. `/fork` spawns an independent background session (keeps the parent cache; managed by `claude attach/logs/stop/respawn`; idle-timeout race fixed 2.1.265); `/subtask` is the in-session variant. A `/fork`/`claude agents` session that changed code commits and pushes before finishing.

## Codex flag fine print

Since 0.154.0: `--worktree` (see watch items), `--ignore-rules`, `--thread-source`, `--dangerously-bypass-hook-trust` exist on `exec`; none are used by the helper.

Standard starts/resumes/forks use [the dispatch helper and run record](references/runtime.md#dispatch-from-one-record). It pins model/effort/cwd and separates artifacts; it does not select a tier or widen the chosen permissions. A 2026-09-11 session audit found two omitted-flag resumes despite the written rule, so these settings are required input rather than inherited intent. Actual runtime settings still require observed evidence. (moved from SKILL.md 2026-09-10)

- `--strict-config` errors on unrecognized keys — pair it with the pins. `codex doctor --json` reports the resolved `model` and version at zero tokens (batch preflight). The planning tool is off by default (`tools.update_plan.enabled = true` restores), so JSONL carries no plan items.
- `--ephemeral` skips session files (forfeits resume — fails loudly); `--add-dir <dir>` grants extra writable roots beyond `--cd`; `-i <file>` attaches images on dispatch and resume.
- `resume --last` is cwd-filtered (`--all` lifts it); exec-spawned threads only have UUIDs; "restore working directory" is TUI-only; resume emits a warning error item when the model resets to the config default, and only the *permission profile* is restored.
- `review --title` needs `--commit` only when used alone.
- Since 0.153.4 astra is the bundled default as well as the server-catalog default (`~/.codex/models_cache.json`); catalog defaults drift without a CLI update — the reason `-m` and `-c model_reasoning_effort` are always passed. Effort scale: `minimal` is pre-5.6 legacy; `ultra` is ChatGPT-auth only (the API rejects it); `persistent` is a binary enum outside the catalog. `gpt-reserve` is a hidden overflow entitlement, not routable. `gpt-6-astra` has a 1.05M context but the whole request reprices 2×/1.5× above 272K input.
- Dispatch chains: join with `;` not `&&` after `git commit` — a no-op commit exits non-zero and kills the chain.
- `browser_use` / `computer_use` / `image_generation` feature flags are stable and enabled; verify on first headless use.
- Terra evidence (whole tier off the ladder since 2026-09-11; `max` since 2026-09-10): DeepSWE +9 pts for +132% cost and +74% wall time over terra `xhigh`; loses to astra `low` outright on Terminal-Bench 4.0 (21.5% vs 50.6%). Terra's React-specific prior (ReactBench) is not used — React legs go to Claude opus `high` instead.
